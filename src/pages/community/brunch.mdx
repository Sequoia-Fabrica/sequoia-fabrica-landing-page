import { promoConstants } from '@/src/text/promoConstants';
import { useEffect } from 'react';

export default function Home() {
  useEffect(() => {
    window.location.href = promoConstants.brunch;
  }, []);

  return (
    <p>
      Redirecting to the wiki... If you're not redirected,
      <a href={promoConstants.brunch}> click here</a>.
    </p>
  );
}
